<!-- app/views/_navbar.html.erb -->
<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Helper Methods 3", root_path, class: "navbar-brand" %>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home",
          movies_path,
          {
            class: "nav-link #{current_page?(movies_path) && "active"}",
            "aria-current": "#{current_page?(movies_path) && "page"}",
          } %>
        </li>
        <li class="nav-item">
          <%= link_to "Link",
          movies_path,
          {
            class: "nav-link #{current_page?(movies_path) && "active"}",
            "aria-current": "#{current_page?(movies_path) && "page"}",
          } %>

        </li>
        <li>
          <%= link_to "Add new movie",
          new_movie_path,
          {
            class: "nav-link #{current_page?(new_movie_path) && "active"}",
            "aria-current": "#{current_page?(new_movie_path) && "page"}",
          } %>
        </li>
      </ul>
    </div>

  </div>
</nav>
